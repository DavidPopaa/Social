import 'package:flutter/material.dart';

class Notificari extends StatelessWidget {
  const Notificari({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Notificari'),
      ),
    );
  }
}
