import 'package:flutter/material.dart';

class despre_postare2_tap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('despre-postare2'),
      ),
    );
  }
}
